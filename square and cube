#include<stdio.h>
int main( )
{
	int num;
	printf(" enter a integer: ");
	scanf("%d",&num);
	int square = num*num;
	int cube= num*num*num;
	printf("square of %d is %d\n", num,square);
	printf("cube of %d is %d\n",num,cube);
	return 0;
}
