#include <stdio.h>
int main() {
    int limit, i;
    printf("Enter the limit: ");
    scanf("%d", &limit);

    printf("All odd numbers between 1 and %d are:\n", limit);
    for (i = 1; i <= limit; i += 2)
        printf("%d\n", i);

    return 0;
}
